color = scaling_value(i,
			get_rgba(0, 0, 0, 255),
			get_rgba(255, 0, 0, 255),
			get_rgba(255, 255, 255, 1)
			);

if (!ft_strncmp(fractal->name, "mandelbort", 5))
		{
			z = (t_complex){fabs(z.x), fabs(z.y)};
			z = sum_complex(sqrt_complex(z), c);
		}
		else

When x/Min/Max ==> -2, 2 \
							-->	0.5
Whne y/Min/Max ==> -2, 2 /

